<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="PharmaStock.Desktop.Views.PackagingUnitPickerPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:PharmaStock.Desktop.Controls"
    xmlns:loc="clr-namespace:PharmaStock.Desktop.Services"
    Title="{loc:Translate UnitPicker_Title}">

    <Grid Padding="24">
        <Border Style="{StaticResource Card}" MaximumWidthRequest="480" HorizontalOptions="Center" VerticalOptions="Center">
            <VerticalStackLayout Spacing="14">
                <Label Text="{loc:Translate UnitPicker_Title}" Style="{StaticResource PageTitle}" />
                <controls:CollapsibleSearchBar x:Name="SearchBarControl" Placeholder="{loc:Translate UnitPicker_SearchPlaceholder}" TextChanged="OnSearchTextChanged" />
                <controls:IconButton Icon="{x:Static loc:BootstrapIcons.PlusLg}" Text="{loc:Translate UnitPicker_New}" Variant="Secondary" Clicked="OnAddUnitNameClicked" />

                <CollectionView x:Name="NamesView" SelectionMode="Single" SelectionChanged="OnNameSelected" HeightRequest="320">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource FieldBorder}" HeightRequest="-1" Padding="12,10" Margin="0,4">
                                <Label Text="{Binding .}" VerticalOptions="Center" />
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
                <Label x:Name="EmptyLabel" Text="{loc:Translate UnitPicker_NoResults}" Style="{StaticResource PageSubtitle}" IsVisible="False" />

                <controls:IconButton Icon="{x:Static loc:BootstrapIcons.XLg}" Text="{loc:Translate Common_Cancel}" Variant="Secondary" Clicked="OnCancelClicked" />
                <ActivityIndicator x:Name="Spinner" IsRunning="False" IsVisible="False" />
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>
