<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    x:Class="PharmaStock.Desktop.Views.CategoryPickerPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:PharmaStock.Desktop.Controls"
    xmlns:loc="clr-namespace:PharmaStock.Desktop.Services"
    Title="{loc:Translate CategoryPicker_Title}">

    <Grid Padding="24">
        <Border Style="{StaticResource Card}" MaximumWidthRequest="480" HorizontalOptions="Center" VerticalOptions="Center">
            <VerticalStackLayout Spacing="14">
                <Label Text="{loc:Translate CategoryPicker_Title}" Style="{StaticResource PageTitle}" />
                <controls:CollapsibleSearchBar x:Name="SearchBarControl" Placeholder="{loc:Translate CategoryPicker_SearchPlaceholder}" TextChanged="OnSearchTextChanged" />
                <controls:IconButton Icon="{x:Static loc:BootstrapIcons.PlusLg}" Text="{loc:Translate CategoryPicker_New}" Variant="Secondary" Clicked="OnAddCategoryClicked" />

                <CollectionView x:Name="CategoriesView" SelectionMode="Single" SelectionChanged="OnCategorySelected" HeightRequest="320">
                    <CollectionView.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource FieldBorder}" HeightRequest="-1" Padding="12,10" Margin="0,4">
                                <Label Text="{Binding Name}" VerticalOptions="Center" />
                            </Border>
                        </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
                <Label x:Name="EmptyLabel" Text="{loc:Translate CategoryPicker_NoResults}" Style="{StaticResource PageSubtitle}" IsVisible="False" />

                <controls:IconButton Icon="{x:Static loc:BootstrapIcons.XLg}" Text="{loc:Translate Common_Cancel}" Variant="Secondary" Clicked="OnCancelClicked" />
                <ActivityIndicator x:Name="Spinner" IsRunning="False" IsVisible="False" />
            </VerticalStackLayout>
        </Border>
    </Grid>
</ContentPage>
