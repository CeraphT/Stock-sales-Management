<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
	<!-- Dev-only: the API is plain HTTP (http://localhost:5080 via adb reverse),
	     and Android blocks all cleartext traffic by default for apps targeting
	     recent SDK versions. Without this, every request silently fails with
	     no useful error — the client just reports "could not reach the server"
	     regardless of whether the tunnel/server are actually fine. Revisit
	     once the API is served over HTTPS for a real deployment. -->
	<application android:allowBackup="true" android:icon="@mipmap/appicon" android:roundIcon="@mipmap/appicon_round" android:supportsRtl="true" android:usesCleartextTraffic="true"></application>
	<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
	<uses-permission android:name="android.permission.INTERNET" />
	<uses-permission android:name="android.permission.CAMERA" />
	<uses-feature android:name="android.hardware.camera" android:required="false" />

	<!-- Receipt printer (BluetoothPrinterTransport): only ever connects to an
	     already-paired device (BondedDevices), never scans for new ones, so
	     no ACCESS_FINE_LOCATION / BLUETOOTH_SCAN is needed — pairing itself
	     happens in Android's own Bluetooth settings, outside this app. -->
	<uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
	<uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
	<uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

	<!-- Receipt printer (UsbPrinterTransport): required="false" so devices
	     without USB-host support (rare among tablets) aren't excluded from
	     installing the app; per-device access is still granted at runtime
	     via UsbManager.RequestPermission, not here. -->
	<uses-feature android:name="android.hardware.usb.host" android:required="false" />
</manifest>