// Intentionally empty — every consumer uses deep imports
// (e.g. `@stockflow/core/api/client`, `@stockflow/core/local/salesService`)
// rather than a single barrel export, so Metro/Vite only bundle what's
// actually used per file.
export {};
