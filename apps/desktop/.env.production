# Production build (npm run tauri build / vite build): the installed desktop app
# talks to the hosted API. Not secret — it's a public URL baked into the client.
VITE_API_BASE_URL=https://api.mfspace.lu
